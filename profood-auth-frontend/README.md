# Profood Auth Frontend

Angular frontend for the Express.js auth service.

## Run

Start the Express auth API first on port 4000, then run:

```bash
npm install
npm start
```

Open: http://localhost:4200
